<div class="row-fluid">
  <div class="span12">
    <h3>Psychic FAQs Categories</h3>

    <% if @categories.any? %>
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th>Category Name</th>
            <th>FAQs</th>
          </tr>
        </thead>
        <tbody>
          <% @categories.each do |category| %>
            <tr>
              <td class="admin-actions">
                <%= link_to '<i class="icon-edit"></i>'.html_safe, edit_admin_psychic_faq_category_path(category) %>
                <%= link_to '<i class="icon-remove"></i>'.html_safe, [:admin, category], data: {confirm: "Are you sure?" }, :method => "delete" %>
              </td>
              <td><%= link_to category.name, edit_admin_psychic_faq_category_path(category) %></td>
              <td><%= category.faqs.count %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%= paginate @categories %>

    <% else %>
      <div class="blank-state">
        No Psychic FAQ Category added yet.
        <%= link_to "Add one now.", new_admin_psychic_faq_category_path %>
      </div>
    <% end %>
    <%= link_to "Add new FAQ Category", new_admin_psychic_faq_category_path, class: "btn btn-small" %>
  </div>
</div>
