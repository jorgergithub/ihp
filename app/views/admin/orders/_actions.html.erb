<% if current_user.website_admin? && @order.refundable? %>
  <%= link_to 'Refund it', admin_order_refunds_path(@order), data: { confirm: "Are you sure?" }, :method => "post", class: 'btn btn-danger' %>
<% end %>

<%= link_to "Back", admin_orders_path, class: 'btn' %>
