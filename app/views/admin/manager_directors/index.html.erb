<div class="row-fluid">
  <div class="span8">
    <h3>Managing Directors</h3>

    <% if @manager_directors.any? %>
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th>Username</th>
            <th>Name</th>
            <th>Email</th>
            <th>Created</th>
          </tr>
        </thead>
        <tbody>
          <% @manager_directors.each do |md| %>
            <tr>
              <td class="admin-actions">
                <%= link_to '<i class="icon-edit"></i>'.html_safe, edit_admin_manager_director_path(md) %>
                <%= link_to '<i class="icon-remove"></i>'.html_safe, [:admin, md], data: {confirm: "Are you sure?" }, :method => "delete" %>
              </td>
              <td><%= link_to md.username, edit_admin_manager_director_path(md) %></td>
              <td><%= md.full_name %></td>
              <td><%= md.email %></td>
              <td><%=l md.created_at, format: :short %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%= paginate @manager_directors %>

    <% else %>
      <div class="blank-state">
        No managing directors added yet.
        <%= link_to "Add one now.", new_admin_manager_director_path %>
      </div>
    <% end %>
    <%= link_to "Add new managing director", new_admin_manager_director_path, class: "btn btn-small" %>
  </div>
</div>
