<section class="container-psychics">
  <h1>Psychics Similar to <%= @psychic.alias_name %>, <b>Available and Ready!</b></h1>

  <% Psychic.first(3).each_with_index do |psychic, i| %>
    <%= render psychic, last: (i+1)%3 == 0 %>
  <% end %>
</section>
